// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "new: card draw 'weight' — a card can be made likelier to come up without crowding everyone else out. Used to rescue the criminal path (its score cards ARE the income, but were drowned in a big draw pool) so pickpockets can actually earn and climb again";
