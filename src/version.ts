// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "card layout: the vertical side-choice labels now stay on a single column (no more mid-word wrap), on top of the earlier fixes — page scrolls to the debug panel/end screen, the centred prompt keeps clear gutters from the choice labels, and debug no longer shrinks the card";
