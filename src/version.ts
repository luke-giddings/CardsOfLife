// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "the streets now have a deck: home_homeless (charity meal, begging for a trickle of income) with four gated exits — rent a room, back to school/grammar/university (by age + credential, once recovered), the workhouse (child), or crawl home (teen). No longer a dead-end spiral";
