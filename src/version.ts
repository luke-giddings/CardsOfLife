// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "bookish now helps at higher education too: a bookish scholar (≥3) pays a gentler cost on the grammar debate and university lectures cards (as it already did at basic school)";
