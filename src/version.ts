// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "apprenticeship: 5 one-shot bench cards, work hard on 3 to pass (skill≥3); trial callable from exp 3; beg-for-more-time defers it (no setback) while cards remain; end card now one-shot";
