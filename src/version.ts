// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "fix: lifestyle cards were locked out on a 'default' lifestyle (old saves that came of age before lifestyle existed) — atMost now treats default as below-all; life_better recovers you to modest; debug label shows ≤ correctly";
