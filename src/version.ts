// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "Tom is much harder to lose: the falling-out now needs a bridge properly burned (not two cold answers), and reaching out to him no longer ends the story — it buys the bond back. Under the hood, gaol and growing out of child labour are content rules again, not engine ones";
