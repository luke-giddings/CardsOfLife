// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "leaving school now goes STRAIGHT to a shop position (shophand), not unemployment — your letters land you a job with a wage from day one (basic + grammar leavers)";
