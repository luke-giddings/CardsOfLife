// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "death preview: a STRUCK-THROUGH skull when a one-shot safety net (charity hospital, sell-up, eviction…) would catch the 0 instead of ending the run; plain skull = real death";
