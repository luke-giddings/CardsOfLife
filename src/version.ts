// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "your years behind bars are now tallied across the whole life and read out in the closing epilogue — along with a line for dying a wanted fugitive if you went over the wall";
