// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "apprenticeship trial now judged on a SKILL trait (built only by working hard), not health; beg-for-more-time 3rd option if unready; gentler give-up. Board-school/factory-closure cards rebalanced; adult transition no longer penalises";
