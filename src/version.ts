// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "apprenticeship no longer traps you: the trade deck is now focused (priority) while you're indentured, so the bench tasks and the qualifying trial come round within ~4 years instead of leaving you stuck for a random decade (or forever) waiting on the draw";
