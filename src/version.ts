// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "balance pass on Sarah's deck: being cold to her now PAYS — her hours go to earning instead of dancing — so shutting her out is a temptation rather than a button that only hurts you";
