// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "end screen reworked: no number score, no routine milestones list — instead 'a life in brief' reads your transient history against your final state, so e.g. being ruined (sold up) and then buying back reads as 'clawed your way back'. Only notable memories surface (the workhouse, the streets, a criminal spell)";
