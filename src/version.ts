// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "tweak: the university trust fund no longer also hands you cash (it just sets up the fund), and the grammar-school job title is now the gender-neutral 'Pupil' instead of 'Grammar-school boy'";
