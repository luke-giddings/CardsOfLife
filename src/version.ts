// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "debug: new Flaws category groups the burden traits, now prefixed flaw* (owesCharity -> flawOwesCharity, soldUp -> flawSoldUp); save migration carries games in progress over";
