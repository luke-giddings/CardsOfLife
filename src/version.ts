// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "fixed the bankruptcy rescue lying about safety: the choice preview no longer counts the rescue card you're answering as its own safety net (it's used up this turn), and 'take to the streets' now drops you out to unemployed so university tuition stops draining you to death the moment you're floored";
