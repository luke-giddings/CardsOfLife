// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "gaol fix: serving your time now actually repeats. The 'no same card twice in a row' rule meant a year served was always followed by the break-out or the cellmate, so escape turned up almost immediately — there are now two copies of the do-your-time card, so the years roll on properly";
