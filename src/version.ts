// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "simplify apprenticeship: trial is now a plain card that unlocks at exp 3 (no milestone, no deferral flag) — it surfaces as the one-shot bench cards deplete; fixes the stray ★ on bench cards";
