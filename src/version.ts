// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "young-adult life-event cards (courting, tavern, thrift, ambition, faith) are now one-shots instead of endless fillers, so the 18–25 deck stops feeling repetitive (the charity-debt card stays a filler — it's meant to recur until you settle up)";
