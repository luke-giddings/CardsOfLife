// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "being sacked, put on the street or sent down now carries the warning sign instead of a gold star; and letting the bully have Tom now actually toughens him, so it is a choice rather than just a cruelty";
