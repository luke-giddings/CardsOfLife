// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "card layout fixes: the page now scrolls to the debug panel (and the end screen) instead of the card swallowing the touch; the prompt is centred with reserved gutters so the choice labels can no longer overlap it; edge labels sit on one tidy line; and turning debug on no longer shrinks and cramps the card";
