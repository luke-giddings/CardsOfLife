// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "running away from the workhouse no longer strands you: it now also flips your job from pauper (which owns no deck) to unemployed, so the job_unemployed deck comes with you and you have a route back to work from the streets (a dedicated homeless deck is still to come)";
