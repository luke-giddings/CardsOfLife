// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "you no longer stay a *child* labourer into your twenties — turning eighteen moves you onto the adult factory floor. Debug: the trait +/- buttons now step by sensible amounts (1 for ages, love and counters) instead of always 10";
