// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "debug deck list is now tidied into collapsible groups (Age / Education / Jobs / Home / Relationships / Other); a collapsed group with a live deck inside shows its header in bold";
