// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "debug traits tidied: Relationships category nests Tom/Sister, plus Education & Jobs groups; traits renamed to sensible prefixes (relBrotherActive/relSisterActive, eduUniFund, jobExperience/jobSkill/jobReachedFactory/jobTimesChanged) with a save migration so games in progress carry over";
