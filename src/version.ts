// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "visible AGE status (baby→child→young adult→adult→old age): passive life-stage drift — baby bonus, then a health decline from 25 that steepens at 50; life-stage decks renamed age_*, old-age stub deck added";
