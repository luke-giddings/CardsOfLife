// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "balance: the apprenticeship path costs more — no free bonus for taking the break, the bench 'work hard' choices now bite (happiness/spirit down), coasting only ever buys happiness, and begging for more time at the trial has a price. Debug card view now shows an option's shown/HIDDEN gate";
