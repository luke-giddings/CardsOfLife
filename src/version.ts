// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "pet-deck balance: the cat is now purely a HAPPINESS companion and the dog purely a SPIRIT one — no more crossing into each other's stat — and the cat's lap card (and the dog's companion/guard cards) are now even trades rather than one lopsided side";
