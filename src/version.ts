// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "death (☠) preview now projects the drains of the status you'd be IN after the choice, not the current ones — a move to a harsher status (homeless/workhouse) shows its real drain";
