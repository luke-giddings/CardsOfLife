// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "status-chip drift symbols now show LADDER POSITION not magnitude: each rung up steps one more pip — housing £- / £-- / £--- / £---- (renting→estate), each career wage +/++/+++ by tier, old age ♥-- past adulthood ♥-. Authored (driftShown), decoupled from the raw numbers";
