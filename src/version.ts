// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "falling out with Tom is back to being possible — the last build set the bar so high the card could never appear. It now needs you to still be cold once he is twelve, not just two early answers. Debug: criminality sits under Jobs now";
