// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "university entry now actually costs: the family fund pays and is spent (uniFund used up), or your own savings foot a heavy tuition bill (finances --) — it was previously free";
