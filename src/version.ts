// On-screen build label. Bump these when it helps to tell builds apart on the
// phone; the git SHA + time are appended automatically (see vite.config.ts).
export const APP_VERSION = "0.1";
export const BUILD_DESC = "homeless 'back to university' exit fixed: it's now for a former undergraduate returning to finish (new eduWasUndergraduate marker), not gated on the uni fund you'd already have spent getting in. Workhouse exit stays children-only";
